#[cfg(test)]
mod tests {
    // Gizmoハンドル検出のテスト
    // 実際のテストは統合テストとして実装する必要があります
    // （Bevy ECS、カメラ、レイキャストが必要なため）

    #[test]
    fn test_gizmo_handle_detection_structure() {
        // 構造の確認
        assert!(true);
    }
}
