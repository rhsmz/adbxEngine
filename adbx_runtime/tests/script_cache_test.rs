#[cfg(test)]
mod tests {
    use adbx_runtime::lua::script_cache::ScriptCache;
    use bevy::prelude::*;

    #[test]
    fn test_script_cache() {
        let cache = ScriptCache::default();
        // キャッシュのテストは実際の実装に依存
    }
}
